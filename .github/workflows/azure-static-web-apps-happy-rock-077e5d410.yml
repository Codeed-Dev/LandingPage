name: Deploy Production

on:
  workflow_dispatch:

jobs:
  production:
      runs-on: ubuntu-latest
      name: Production
      steps:
        - uses: actions/checkout@v2
          with:
            submodules: true
        - name: Build And Deploy on Production
          id: builddeploy
          uses: Azure/static-web-apps-deploy@v1
          with:
            azure_static_web_apps_api_token: ${{ secrets.AZURE_STATIC_WEB_APPS_API_TOKEN_HAPPY_WATER_0EF9D4610 }}
            repo_token: ${{ secrets.GITHUB_TOKEN }}
            action: "upload"
            app_location: "/"
            output_location: "out"
